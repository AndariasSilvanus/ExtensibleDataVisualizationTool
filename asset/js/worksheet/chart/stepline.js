var optikos_chart = {};

optikos_chart.chart = {
	type: 'line'
};

optikos_chart.plotOptions= {
    series: {
        step: 'left' // or 'center' or 'right'
    }
};

optikos_chart.dimensionQuantity = 1;
optikos_chart.measureQuantity = 1;